# Cloudflare Workers + Assets — minimal static deploy.
# No `main`, no extras — only the fields Cloudflare strictly requires.
# If Cloudflare still complains, delete this file and use the GitHub Pages
# deployment instead (.github/workflows/pages.yml).

name = "cardiotrack-dashboard"
compatibility_date = "2024-09-01"

[assets]
directory = "./Data/Output"
